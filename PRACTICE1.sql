SELECT ENAME , JOB , SAL
FROM EMP 
WHERE SAL>2500;

SELECT * 
FROM EMP
WHERE DEPTNO IN (10,20);

SELECT ENAME, SAL
FROM EMP
WHERE SAL NOT BETWEEN 1000 AND 3000;

SELECT ENAME
FROM EMP
WHERE JOB IS NOT "CLERK";

SELECT ENAME, SAL+SAL*10/100 SALARY_WITH_10HIKE
FROM EMP;

SELECT ENAME 
FROM EMP
WHERE ENAME LIKE '%A%';

descending. 
SELECT *
FROM EMP
ORDER BY DEPTNO ASC, SAL DESC;

SELECT  COUNT(*),DEPTNO
FROM EMP
GROUP BY DEPTNO;

SELECT DEPTNO, SUM(SAL) AS TOTAL_SALARY
FROM EMP
GROUP BY DEPTNO
HAVING SUM(SAL)>9000;

SELECT JOB,AVG(SAL)
FROM EMP 
GROUP BY JOB;

SELECT *
FROM EMP
WHERE COMM IS NOT NULL;

SELECT * 
FROM EMP
WHERE TO_CHAR(HIREDATE,'MM')='12';

